@page "/transactions"
<h3>Transactions</h3>
<p class="muted">Daftar transaksi — filter: tanggal, tipe (Income/Expense/Transfer), akun, kategori. Data dari <code>GET /api/v1/transactions</code> (butuh token Syafri).</p>
<div class="panel"><p class="muted">Belum ada transaksi. Tambah via Mobile atau <code>POST /api/v1/transactions</code>.</p></div>
