@page "/accounts"
<h3>Accounts</h3>
<p class="muted">Cash, Bank, EWallet — balance dihitung dari transaksi.</p>
<div class="panel"><p class="muted">Buat akun via <code>POST /api/v1/accounts</code> (nama, tipe, currency IDR).</p></div>
