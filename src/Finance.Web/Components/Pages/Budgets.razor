@page "/budgets"
<h3>Budgets</h3>
<p class="muted">Budget per kategori + bulan — format <code>Category + Period + Limit</code>.</p>
<div class="panel"><p class="muted">Buat via <code>POST /api/v1/budgets</code> (month=2026-09, categoryId, amount).</p></div>
